# Core dependencies — light install, enough for Phase 1 baselines + eval.
# NumPy pinned <2: torch 2.2.x (the last torch supporting Python 3.9) is built
# against NumPy 1.x and crashes on tensor<->numpy conversions with NumPy 2.
numpy>=1.24,<2
pandas>=2.0
pyarrow>=12.0            # parquet read/write for the data layer
scipy>=1.10
scikit-learn>=1.3
implicit>=0.7            # ALS matrix factorization for implicit feedback
tqdm>=4.65
pyyaml>=6.0

# EDA / notebooks
matplotlib>=3.7
seaborn>=0.12
jupyterlab>=4.0
ipykernel>=6.25
